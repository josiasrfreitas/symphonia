# Copy to ~/.symphonia/.env (reaches every role, in any worktree) or to
# ./.env (this checkout only). The shell always wins over both.
# Linear -> Settings -> Security & access -> Personal API keys
LINEAR_API_KEY=lin_api_xxxxxxxxxxxxxxxxxxxxxxxx
