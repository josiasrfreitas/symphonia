# Left over from installs before the registry moved out of the checkout.
# The live one is ~/.symphonia/runtime/spawns.json (SYMPHONIA_RUNTIME
# overrides it) — a role reads it from the ticket's worktree, which is a
# different copy of this repository, so it cannot live in here.
.runtime/
__pycache__/
